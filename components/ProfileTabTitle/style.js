import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  title: {
    fontSize: 14,
    fontFamily: 'Inter',
    lineHeight: 18,
    color: 'black',
  },
});

export default style;
