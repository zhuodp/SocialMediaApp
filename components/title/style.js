import { StyleSheet } from "react-native";


const style = StyleSheet.create({
    title: {
        fontFamily: 'Inter',
        fontWeight: '600',
        fontSize: 24,
        lineHeight: 25,
    },
});

export default style;
